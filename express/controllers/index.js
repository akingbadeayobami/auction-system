"use strict";
const users = require('./users');
const inventories = require('./inventories');
const auctions = require('./auctions');

/** @namespace */
module.exports = {
    users,
    inventories,
    auctions,
};